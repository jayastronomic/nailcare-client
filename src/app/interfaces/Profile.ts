export default interface Profile {
  firstName: string;
  middleInital?: string;
  lastName: string;
  birthdate: string;
  city: string;
  state: string;
  zipCode: string;
  unitNumber?: string;
  streetAddress: string;
}
